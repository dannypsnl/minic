(* TODO:
   We have stack pointer as possible virtual register now.

   1. if instruction's dest is a stack pointer, we need to add a store instruction.
   2. if instruction's any source is a stack pointer, we need to add a load instruction.
*)
